# --- Reputation lookups (optional; triage degrades gracefully without them)
VIRUSTOTAL_API_KEY=
MALWAREBAZAAR_API_KEY=

# --- LLM reasoning (optional; falls back to deterministic template explanations)
ANTHROPIC_API_KEY=
NULLIFY_LLM_MODEL=claude-sonnet-4-6

# --- Sandbox for --deep dynamic analysis (CAPEv2 REST endpoint). Samples are
#     NEVER executed locally — without this, deep scans are skipped safely.
NULLIFY_SANDBOX_URL=
